# Backend Port
PORT=5000

# MySQL Database Configuration (Optional: system falls back to embedded data if MySQL is absent)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=mathur_giri_math_db
DB_PORT=3306
# DB_SSL=true

# Or Single Connection String for Cloud MySQL (Aiven, PlanetScale, Railway, etc.)
# MYSQL_URL=mysql://user:password@host:3306/dbname

# Admin Secret Key for Committee Registration
ADMIN_SECRET_KEY=GOTEGAON_MATH_2026

# JWT Authentication Secret Key
JWT_SECRET=mathur_giri_gotegaon_secret_key_2026
